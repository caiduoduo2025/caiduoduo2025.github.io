# Windows电脑快速入门

欢迎使用 **财多多** —— 你的智能投资理财管家。

请在电脑上按照以下步骤快速开始使用，客服 **微信号**：`caiduoduokefu`：

## 1. Java 环境准备

本软件依赖 Java 运行环境，请确保你的电脑已安装 **Java 21** 或更高版本。

### 下载安装包
- **64位系统**: [点击下载 Java 21 (x64 Installer)](https://download.oracle.com/java/21/latest/jdk-21_windows-x64_bin.exe)
  > *注：Java 21 仅支持 64 位 Windows 系统。*

### 验证安装

安装完成后，请按照以下步骤验证：

1. **打开终端**：
   按 `Win + R` 键，输入 `cmd` 并回车。

2. **输入命令**：
   在终端窗口中输入以下命令并回车，检查 Java 版本：
   ```bash
   java -version
   ```

3. **检查输出**：
   如果看到类似 `java version "21.0.x"` 显示，说明安装成功。

   > **Java 版本问题**：如果显示版本低于 21（如 1.8 或 11），软件将无法启动。请卸载旧版本：
   > 打开“设置” > “应用”，搜索 Java 并卸载旧版本。

## 2. 下载财多多软件

点击下方链接下载最新版本财多多软件：

- [下载 cdd-1.0.jar](https://cdd-dl-1302109275.cos.ap-guangzhou.myqcloud.com/cdd-1.0.jar)

## 3. 启动财多多软件

1. **找到下载的文件**：
   打开文件资源管理器，进入 **Downloads** 目录（通常文件会下载到这里），找到 `cdd-1.0.jar` 文件。

2. **右键运行**：
   鼠标右键点击 `cdd-1.0.jar` 文件，选择 **“打开方式”** -> **“Java(TM)”**。

   > *注意：如果没有看到 Java 选项，说明未成功安装 Java。*

3. **等待启动**：
   启动后，浏览器会自动打开 `http://localhost:8888`。

   如果浏览器没有自动打开，请手动访问上述地址，你将看到财多多软件的激活页面。

## 4. 获取财多多激活码

请添加客服微信申请激活码：

- **微信号**：`caiduoduokefu`
- **申请备注**：请务必备注你的**邮箱地址**（用于接收激活码）。

## 5. 激活财多多

关注你的邮箱，请在收到激活码 **72小时内** 完成激活。

## 6. 开始使用财多多

激活成功后，**在浏览器中**你可以立即开始：

- **创建组合**：建立你的第一个投资组合。
- **入金记录**：记录你的初始资金。
- **添加资产**：添加你持有的股票、基金或其他资产。
- **交易记录**：记录买卖操作。
- **组合分析**：查看收益曲线和资产分布。

祝你使用愉快，财源滚滚！

